<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bet %>
    <%= form.number_field :bet %>
  </div>

  <div class="field">
    <%= form.label :rule_match %>
    <%= form.number_field :rule_match %>
  </div>

  <div class="field">
    <%= form.label :rule_goal_difference %>
    <%= form.number_field :rule_goal_difference %>
  </div>

  <div class="field">
    <%= form.label :rule_tendency %>
    <%= form.number_field :rule_tendency %>
  </div>

  <div class="field">
    <%= form.label :rule_miss %>
    <%= form.number_field :rule_miss %>
  </div>

  <div class="field">
    <%= form.label :private %>
    <%= form.check_box :private %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
