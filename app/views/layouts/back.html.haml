- replace :layout, 'back'
- replace :body_class, 'myg-layout--toolbar'

= mozaic do

  = component 'myg/toolbar' do
    .mdc-toolbar__row
      = link_to area(:back_location, url_for(:back)), data: { turbolinks_animate_appear: 'fadein' } do
        %i.nc.nc-tail-left-2.nc-lg

  .myg-content
    = mozaic_area :back do
      = yield
