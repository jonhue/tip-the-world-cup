- replace :layout, 'back'
- replace :body_class, 'myg-layout--toolbar'

= mozaic do

  = component 'myg/toolbar' do
    .mdc-toolbar__row
      = link_to area(:back_location, url_for(:back)) do
        %i.nc.nc-tail-left-2.nc-lg

  .myg-content
    = mozaic_area :back do
      = yield
